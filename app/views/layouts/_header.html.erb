<header>
  <% if signed_in? %>
    [<%= current_user.email %>]
    [<%= button_to 'sign out', destroy_user_session_url, method: :delete %>]
  <% else %>
    [<%= link_to 'account', new_user_session_url %>]
  <% end %>
</header>
